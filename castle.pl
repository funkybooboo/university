% Base case, empty list, do nothing
printList([]).
printList(H | T) :-
    write(H), nl,
    printList(T).

% Add your comments here
solveRooms(Castle, L) :- 
  printList(NL).


% Add your comments here
solveRoomsWithinCost(Castle, Limit) :- 
  printList(NL).

